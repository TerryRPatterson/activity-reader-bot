from setuptools import setup, find_packages

setup(
    name='Activity Reader Bot',
    version='1.0',
    packages=find_packages(),
    author='PortalPirate',
    author_email='PortalPirate@wegrok.net',
    description='This is a bot that counts messages',

)